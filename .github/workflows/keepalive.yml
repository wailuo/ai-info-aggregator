name: Keep WeWe RSS Alive

on:
  schedule:
    - cron: "*/20 * * * *"  # 每 20 分钟 ping 一次

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping WeWe RSS
        run: curl -sf https://app-production-ad4a.up.railway.app/ || true
